PORT=3004
DATABASE_URL="postgresql://notify_user:notify_pass@localhost:5436/notify_db?schema=public"

# MailHog SMTP (local)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM="no-reply@clinicflow.com"

# CORS (local UI)
CORS_ORIGINS=http://localhost:3005
CORS_CREDENTIALS=false
